# Firebase deployment requirements (optimized)
fastapi==0.104.1
uvicorn[standard]==0.24.0
scikit-learn==1.3.2
numpy==1.26.2
pandas==2.1.3
httpx==0.25.2
cachetools==5.3.2
python-multipart==0.0.6
aiohttp==3.9.1
textblob==0.17.1
pydantic==2.5.0

# Keep lightweight - skip heavy dependencies for Firebase
# torch  # Skip for Firebase (too large)
# tensorflow  # Skip for Firebase

# Essential packages only
requests==2.31.0
python-dotenv==1.0.0
certifi==2024.2.2

# Gemini AI integration (lightweight)
google-generativeai==0.3.2